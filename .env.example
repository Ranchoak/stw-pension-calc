# Supabase is optional for now — the calculator runs fully client-side.
# When we add saved scenarios / auth, copy this to .env.local and fill in
# the values from your Supabase project settings (Settings → API).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
